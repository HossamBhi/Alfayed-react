export enum profileEnums {
  suppliers = 1,
  expenses,
  fridges,
  clients,
  employees,
}

export enum trasactionsEnums {
  pay = 2,
  bouns,
  income,
}
export enum fridgeTransactionEnums {
  in = 1,
  out,
}
